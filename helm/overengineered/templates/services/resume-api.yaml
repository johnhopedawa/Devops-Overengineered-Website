apiVersion: v1
kind: Service
metadata:
  name: resume-api-service
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: resume-api
  ports:
    - port: 3001
      targetPort: 3001
